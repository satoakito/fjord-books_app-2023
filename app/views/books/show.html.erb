<p style="color: green"><%= notice %></p>
<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to t('activerecord.attributes.book.edit'), edit_book_path(@book) %> |
  <%= link_to t('activerecord.attributes.book.back'), books_path %>

  <%= button_to t('activerecord.attributes.book.destroy'), @book, method: :delete %>
</nav>
